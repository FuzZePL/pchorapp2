class Server {
  bool auth(String email, String password) {
    return true;
  }

  bool createAccount(
    String email,
    String password,
    String name,
    String surname,
  ) {
    return true;
  }
}
